# OpenClaw 社区版 — 环境变量
# 复制为 .env 后填入自己的 key；留空则对应能力不启用。
# 未填的占位符不会被写进配置（安装器会过滤）。

# ── 必填：主模型 ──
# DeepSeek 控制台 https://platform.deepseek.com/ 获取
DEEPSEEK_API_KEY=YOUR_DEEPSEEK_API_KEY

# ── 可选：OpenAI 兼容端点（部分脚本会用）──
OPENAI_API_KEY=

# ── 可选：地图服务（amap-service 技能）──
# 高德开放平台 https://lbs.amap.com/ 申请 Web服务 key
AMAP_API_KEY=

# ── 可选：A股数据（stock_data_service）──
# Tushare https://tushare.pro/user/token 获取；留空则只用 akshare/baostock 免费源
TUSHARE_TOKEN=

# ── 网关 token ──
# 留空则安装时自动生成随机 token（推荐）
OPENCLAW_GATEWAY_TOKEN=

# ── 本地向量检索（无需 key）──
# 想让 memorySearch 生效：安装 ollama 后执行 ollama pull bge-m3
# 不想装就把 openclaw.json 里 agents.defaults.memorySearch.enabled 改为 false
